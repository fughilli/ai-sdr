load("@rules_python//python:defs.bzl", "py_test")

# Hardware-free smoke test. Requires `nix` on PATH (pulls in the @grenv env via
# //sdr:sdr), but no HackRF. Run: bazel test //tests:smoke_test
py_test(
    name = "smoke_test",
    srcs = ["smoke_test.py"],
    deps = ["//sdr:sdr"],
)
